# Current separated pilot

The current default-A pilot is defined in [configs/recurrent.py](../../../configs/recurrent.py). It is a short MPS run on `smoke_real`, not an automatic longer continuation of the completed ablation. Run `uv run python train.py configs/recurrent.py` from the repository root. New artifacts go in this directory's ignored `results/`. No run was launched during repository cleanup.
